{
  "name": "react-hooks-fetch-crud-lab",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    // ... your dependencies
  },
  "scripts": {
    // ... your scripts
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "jest": {
    "transformIgnorePatterns": [
      "node_modules/(?!(?:.pnpm/)?@adobe/css-tools/.*)"
    ]
  }
}